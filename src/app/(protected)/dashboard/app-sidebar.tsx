'use client'

import { Sidebar, SidebarHeader } from "@/components/ui/sidebar";

export function AppSidebar() {
    return (
        <Sidebar>
            Logo
            </SidebarHeader>
        </Sidebar>
    );
}